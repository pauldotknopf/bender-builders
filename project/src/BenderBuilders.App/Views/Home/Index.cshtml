<div id="app">
    sdf
    <button @@click="count++">
        Count is: {{ count }}
    </button>
</div>
    
<div>test</div>
    
<script type="text/javascript">
        debugger;
        const createApp = window.Vue.createApp;
        const ref = window.Vue.ref;
        createApp({
            setup() {
                return {
                    count: ref(0)
                }
            }
        }).mount('#app')
    </script>